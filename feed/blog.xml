---
layout:
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ '/feed/blog.xml' | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ '/feed/blog.xml' | absolute_url | xml_escape }}</id>
  <title type="html">{{ site.title | xml_escape }}</title>
  {% if site.description %}
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  {% endif %}
  {% assign posts = site.pages | where: "post", true | where_exp: "item", "item.draft != true" | sort: "date" | reverse %}
  {% for post in posts limit: 20 %}
  <entry>
    <title type="html">{{ post.title | smartify | strip_html | normalize_whitespace | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.url | absolute_url | xml_escape }}</id>
    <content type="html" xml:base="{{ post.url | absolute_url | xml_escape }}"><![CDATA[{{ post.content | markdownify | strip }}]]></content>
    {% assign post_summary = post.description | default: post.excerpt %}
    {% if post_summary %}
    <summary type="html"><![CDATA[{{ post_summary | markdownify | strip_html | normalize_whitespace }}]]></summary>
    {% endif %}
    {% for tag in post.tags %}
    <category term="{{ tag | xml_escape }}" />
    {% endfor %}
  </entry>
  {% endfor %}
</feed>
